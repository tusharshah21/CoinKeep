import {
  pgTable,
  timestamp,
  text,
  integer,
  boolean,
  index,
  uniqueIndex,
  uuid,
  bigint,
  varchar,
  primaryKey,
  unique,
  jsonb,
} from "drizzle-orm/pg-core";
import { relations } from "drizzle-orm";
